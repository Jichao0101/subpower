# knowledge writeback closure

This fixture demonstrates closure-passed knowledge writeback artifacts.

It contains:

- a verified current-knowledge candidate that can end in a staged receipt
- an unverified current-knowledge candidate that must be declined

The fixture uses logical destination refs only. It does not write to or reference an external knowledge base path.
