{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "workflow_plan",
  "type": "object",
  "required": ["session_id", "producer_agent", "workflow_id", "planned_steps"],
  "additionalProperties": false,
  "properties": {
    "session_id": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]*$"},
    "producer_agent": {"type": "string", "minLength": 1},
    "workflow_id": {"enum": ["incident_investigation", "bug_fix", "board_validation", "functional_review", "knowledge_writeback"]},
    "planned_steps": {"type": "array", "minItems": 1, "items": {"type": "string", "minLength": 1}}
  }
}
