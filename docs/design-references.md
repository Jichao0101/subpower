# design references

subpower documentation is self-contained. External implementations may inform engineering judgment, but they are not normative inputs for subpower users.

No source-level, runtime-level, schema-level, or artifact-level dependency on an external orchestration project is allowed.
