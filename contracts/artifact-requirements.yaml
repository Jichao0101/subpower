{
  "schema": "subpower.artifact-requirements/v1",
  "artifacts": {
    "prompt_context": {"required_fields": ["session_id", "producer_agent", "observed_symptom", "expected_behavior", "validation_criteria", "repo_scope", "board_context", "missing_context", "assumptions"]},
    "task_profile": {"required_fields": ["session_id", "producer_agent", "primary_type", "task_goal"]},
    "workflow_plan": {"required_fields": ["session_id", "producer_agent", "workflow_id", "planned_steps"]},
    "workflow_state": {"required_fields": ["session_id", "producer_agent", "current_workflow", "current_phase", "active_role", "pending_artifacts", "completed_artifacts", "open_decision_points", "status"]},
    "subagent_execution_status": {"required_fields": ["session_id", "producer_agent", "subpower_invoked", "subagent_execution_required", "subagent_execution_mode", "spawned_roles", "not_spawned_roles", "fallback_used", "fallback_reason", "degraded", "degradation_reason", "independence_guarantee"]},
    "agent_invocation_manifest": {"required_fields": ["session_id", "producer_agent", "invocations"]},
    "side_state": {"required_fields": ["session_id", "producer_agent", "sides"]},
    "handoff_packet": {"required_fields": ["session_id", "from_agent", "to_agent", "artifacts"]},
    "incident_report": {"required_fields": ["session_id", "producer_agent", "observed_symptom", "evidence_refs", "summary"]},
    "root_cause_hypotheses": {"required_fields": ["session_id", "producer_agent", "hypotheses"]},
    "next_workflow_recommendation": {"required_fields": ["session_id", "producer_agent", "recommended_workflow", "reason", "required_artifacts"]},
    "implementation_plan": {"required_fields": ["session_id", "producer_agent", "repo_scope", "verification_plan"]},
    "code_change_manifest": {"required_fields": ["session_id", "producer_agent", "files_changed", "verification_results"]},
    "review_decision": {"required_fields": ["session_id", "producer_agent", "decision", "findings"]},
    "board_target": {"required_fields": ["session_id", "producer_agent", "source", "target_id", "executor_type", "log_paths", "collect_paths", "run_commands", "expected_behavior", "validation_criteria", "constraints"]},
    "board_session": {"required_fields": ["session_id", "producer_agent", "board", "scenario", "status"]},
    "board_validation_result": {"required_fields": ["session_id", "producer_agent", "status", "evidence_refs"]},
    "board_failure_review": {"required_fields": ["session_id", "producer_agent", "failure_class", "failure_type", "recommended_next", "recommended_route", "confidence", "blocking_evidence", "supporting_evidence_refs", "rationale"]},
    "main_route_decision": {"required_fields": ["session_id", "producer_agent", "decision_point_id", "assessor_artifact", "route", "reason", "based_on_artifacts"]},
    "route_history": {"required_fields": ["session_id", "producer_agent", "routes"]},
    "evidence_manifest": {"required_fields": ["session_id", "producer_agent", "status", "evidence"]},
    "closure_matrix": {"required_fields": ["session_id", "producer_agent", "close_allowed", "review_status", "evidence_status", "repo_state", "board_state", "knowledge_state", "blockers"]},
    "knowledge_writeback_candidate": {"required_fields": ["session_id", "producer_agent", "target_scope", "title", "summary", "claims", "evidence_refs", "source_artifacts", "risks", "boundaries"]},
    "writeback_plan": {"required_fields": ["session_id", "producer_agent", "candidate_artifact", "target_scope", "destination_ref", "evidence_refs", "requires_host_write", "planned_steps"]},
    "writeback_receipt": {"required_fields": ["session_id", "producer_agent", "candidate_artifact", "plan_artifact", "status", "target_scope", "writeback_refs", "evidence_refs", "external_write_performed_by_subpower"]},
    "writeback_declined": {"required_fields": ["session_id", "producer_agent", "candidate_artifact", "reason", "blockers", "evidence_refs"]}
  }
}
